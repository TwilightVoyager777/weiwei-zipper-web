import { Footer } from 'weiwei-zipper-web';

// 站点页脚 — dark panel with brand logo, product/quick-link columns and
// contact info. Content is fixed (zh catalog + site data); no props.
export const SiteFooter = () => <Footer />;
