import { Header } from 'weiwei-zipper-web';

// 站点顶部导航 — sticky header with brand logo, main nav (产品中心 dropdown
// trigger), contact info and language switcher. No props; zh locale content.
export const SiteHeader = () => <Header />;
